pandas>=1.5.3
numpy>=1.24.0
TA-Lib==0.4.24
streamlit>=1.20.0
plotly>=5.0.0
matplotlib>=3.5.0
openpyxl>=3.0.10
xlsxwriter>=3.0.0
xlrd>=2.0.1
yfinance>=0.2.22
scipy>=1.9.0
scikit-learn>=1.2.0
python-dateutil>=2.8.2
pytz>=2023.3
requests>=2.28.0
typing-extensions>=4.5.0

# Optional (only if you use fancy Excel/office integrations)
# pywin32>=305

# Notes:
# - TA-Lib is a C extension and may require system-level dependencies on Windows. If you can't
#   install the binary wheel, consider using the `ta` Python package or `pandas_ta` as alternatives.
# - Pin versions to match your environment if needed.
